import styled from '@emotion/styled'
import { Grid } from '@mui/material'

export const StyledStepGrid = styled(Grid)`
  && {
    display: flex;
    align-items: center;
    gap: 12px;
    max-width: 440px;
    width: 100%;
    flex-wrap: nowrap;
    margin-top: 0;
    margin: 20px 0 40px 0;
  }
`
